{
  "name": "fe-swe-take-home-test-kanastra-nicolas-lima",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "check": "biome check --changed --since=origin/main src/*",
    "check:apply": "biome check --changed --since=origin/main src/* --apply",
    "lint": "biome lint --changed --since=origin/main src/*",
    "lint:apply": "biome lint --changed --since=origin/main src/* --apply",
    "format": "biome format --changed --since=origin/main",
    "format:write": "biome format --changed --since=origin/main --write"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react-swc": "^3.5.0",
    "globals": "^15.9.0",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}
